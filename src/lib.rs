//! # Git Repository Manager (GRM)
//! Small command-line tool to find local Git repositories and report that status of them.

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
